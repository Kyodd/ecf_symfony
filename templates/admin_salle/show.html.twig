{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ salle.nom }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ salle.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <div class="btn">
        <a class='btn' href="{{ path('app_admin_salle_index') }}">back to list</a>
        <a  class='btn' href="{{ path('app_admin_salle_edit', {'id': salle.id}) }}">edit</a>
      {{ include('admin_salle/_delete_form.html.twig') }}
        </div>
{% endblock %}
