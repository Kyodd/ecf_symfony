{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>{% trans %}Nom{% endtrans %}</th>
                <td>{{ salle.nom }}</td>
            </tr>
            <tr>
                <th>{% trans %}Capacité{% endtrans %}</th>
                <td>{{ salle.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <div class="btn">
        <a class='btn' href="{{ path('app_admin_salle_index') }}">{% trans %}Retour à la liste{% endtrans %}</a>
        <a  class='btn' href="{{ path('app_admin_salle_edit', {'id': salle.id}) }}">{% trans %}Modifier{% endtrans %}</a>
      {{ include('admin_salle/_delete_form.html.twig') }}
        </div>
{% endblock %}
