<form method="post" action="{{ path('app_admin_salle_delete', {'id': salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.id) }}">
   
    <button style='display:flex' class="btn"> <i class="material-icons">delete</i> {% trans %}Modifier{% endtrans %}</button>
</form>
